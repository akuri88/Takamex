<!DOCTYPE html>
<html>
	<head>
		<title>Takamex</title>
		<%= csrf_meta_tag %>
		<%= render 'layouts/stylesheets' %>
	</head>
	<body>

		<div class="container">
			<%= render 'layouts/header' %>
			<%= render 'layouts/social' %>
			<section class="main">
				<%= yield %>
			</section>
		</div>

	</body>
</html>
